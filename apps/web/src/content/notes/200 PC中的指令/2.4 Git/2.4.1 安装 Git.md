---
title: 2.4.1 安装 Git
description: ''
tags: []
draft: false
createdAt: '2026-04-04T11:44:09.241Z'
updatedAt: '2026-02-25T05:20:26.114Z'
sourcePath: 200 PC中的指令/2.4 Git/2.4.1 安装 Git.md
---
# 1. Windows 下载安装 git 客户端


- 在 PowerShell 中使用 `WinGet` 安装 Git

`WinGet` 是是微软为 Windows 开发的**官方命令行软件包管理器**。一般来说，Win11 系统自带 `WinGet`，打开 PowerShell 直接执行命令行即可

```powershell
winget --version                                  #查询WinGet版本号               
winget install --id Git.Git -e --source winget    #一键安装Git
```

- `--id Git.Git`：锁定官方发布的 Git 标识符。
- `-e`：精确匹配标识符。
- `--source winget`：强制从微软官方源下载，安全性更有保障。

- 刷新环境变量

**这是关键一步：** 在 CMD 中，你必须重启窗口。但在 PowerShell 中，你可以直接运行该命令刷新环境变量，无需关闭窗口即可让 `git` 命令生效

```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

下载成功后查询 `git` 的版本和更新

```powershell
git --version         #查看版本
winget upgrade git    #使用winget一键更新git
```

# 2. Mac 下载安装 git 客户端

先安装包管理器 [Homebrew](https://brew.sh/)，然后通过 `Homebrew` 安装 Git

```bash

$ brew install git
```


# 3. Linux 下载安装 git 客户端

Debian 或 Ubuntu Linux

```bash
sudo apt install git  
```

RedHat Linux

```bash
sudo yum install git
```

# 4. 配置 Git

全局配置，在命令行输入：

```bash
$ git config --global user.name "你的名字"				
$ git config --global user.email "你的邮箱@example.com"	
```
- `--global`：这台机器上所有的 Git 仓库都会使用这个配置
